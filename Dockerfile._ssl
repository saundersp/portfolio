FROM node

WORKDIR ./portfolio/build

RUN npm install -g http-server

COPY build /usr/portfolio
COPY certificates /usr/certificates

EXPOSE 443

CMD ["http-server", "/usr/portfolio", "-p", "443", \
	"-S", "-C", "/usr/certificates/cert.pem", \
	"-K", "/usr/certificates/privkey.pem"]